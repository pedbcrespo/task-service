export default class Address {
    constructor(state, city, district, street) {
        this.state = state;
        this.city = city;
        this.district = district;
        this.street = street;
    }
}